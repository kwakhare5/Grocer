[pytest]
asyncio_mode = auto
testpaths = backend/tests
pythonpath = .
markers =
    live_llm: tests that invoke live external LLM APIs (deselected by default to protect API quotas)
addopts = -m "not live_llm"
